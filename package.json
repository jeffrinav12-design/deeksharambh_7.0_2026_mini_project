{
  "name": "deeksharambh-bridge-course-management-system",
  "version": "1.0.0",
  "description": "Deeksharambh Bridge Course Management System for Sankara College of Science and Commerce",
  "main": "api/index.js",
  "type": "module",
  "scripts": {
    "install-all": "npm install && npm install --prefix server && npm install --prefix client",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "seed": "node server/scripts/seed.js",
    "build": "cd client && npm install && npm run build && node -e \"import('fs').then(fs => fs.cpSync('dist', '../dist', {recursive: true}))\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
